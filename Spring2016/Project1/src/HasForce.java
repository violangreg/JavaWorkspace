/**
 * Interface HasForce, used for additional methods
 * @author Greg
 */
public interface HasForce {
	/**
	 * used to for Overriding this to implented classes
	 * @param e, passes in the entity, which is the target
	 */
	public void useForce(Entity e);
}
